<Window x:Class="Cinebel.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cinebel"
        mc:Ignorable="d"
        Icon="/Images/video.ico"
        Title="Window1" Height="700" Width="1200" ResizeMode="NoResize" DataContext="{StaticResource Choice}">
    <Window.Background>
        <ImageBrush ImageSource="/Images/Cine.jpg" Stretch="Fill" />
    </Window.Background>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="61*"/>
            <ColumnDefinition Width="539*"/>
        </Grid.ColumnDefinitions>

        <TabControl Grid.ColumnSpan="2">
            <TabItem Header="Liste des films">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="67*"/>
                        <ColumnDefinition Width="19"/>
                        <ColumnDefinition Width="12*"/>
                        <ColumnDefinition Width="1096*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="titre" HorizontalAlignment="Left" Margin="3,136,0,0" VerticalAlignment="Top" Grid.Column="2" Grid.ColumnSpan="2"/>
                    <Label Content="Synopsis" HorizontalAlignment="Left" Margin="0,206,0,0" VerticalAlignment="Top" Grid.Column="2" Grid.ColumnSpan="2" />

                    <Label Content="Date de sortie" HorizontalAlignment="Left" Margin="323,46,0,0" VerticalAlignment="Top" Grid.Column="3"/>

                    <TextBox HorizontalAlignment="Left" Margin="70,140,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" Text="{Binding Titre}" Grid.Column="3" />
                    <TextBox HorizontalAlignment="Left" Margin="70,214,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" Text="{Binding Synopsis}" Grid.Column="3" />
                    <Button Content="Ajouter un Film " HorizontalAlignment="Left" Margin="439,566,0,0" VerticalAlignment="Top" Height="46" Width="91" Command="{Binding Ajouterfilm}" Grid.Column="3">

                    </Button>
                    <DatePicker HorizontalAlignment="Left" Text="{Binding AnneeDeSortie}" Margin="439,59,0,0" VerticalAlignment="Top" Grid.Column="3"/>


                    <!--Genre-->
                    <!--Genre-->


                    <Label Content="Genre" HorizontalAlignment="Left" Margin="735,59,0,0" VerticalAlignment="Top" Grid.Column="3"/>
                    <ListView  SelectionMode="Single" Margin="811,72,108,431" ItemsSource="{Binding TousGenre}"  SelectedItem="{Binding Genres, Mode=TwoWay}" Grid.Column="3" >
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nr°" Width="30" DisplayMemberBinding="{Binding Id}"></GridViewColumn>
                                <GridViewColumn Header="Nom" Width="170" DisplayMemberBinding="{Binding NomGenre}" ></GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>


                    <!--ScenaristeScenaristeScenaristeScenariste-->
                    <!--ScenaristeScenaristeScenaristeScenariste-->


                    <Label Content="Scenariste" HorizontalAlignment="Left" Margin="18,328,0,0" VerticalAlignment="Top" Grid.ColumnSpan="2"/>
                    <ListView SelectionMode="Single"  Margin="7,328,863,187" ItemsSource="{Binding TousScenariste}" SelectedItem="{Binding ScenaristeSelecter, Mode=TwoWay}" Grid.Column="3" >
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nr°" Width="30" DisplayMemberBinding="{Binding Id_Personne_scen }" ></GridViewColumn>
                                <GridViewColumn Header="Nom" Width="100" DisplayMemberBinding="{Binding Nom_scen }"></GridViewColumn>
                                <GridViewColumn Header="Prenom" Width="100" DisplayMemberBinding="{Binding Prenom_scen }"></GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <Label  Content="Realisateur" HorizontalAlignment="Left" Margin="666,0,0,0" VerticalAlignment="Center" Grid.Column="3"/>

                    <ListView SelectionMode="Single"  Margin="775,312,108,207" ItemsSource="{Binding TousRealisateur}" SelectedItem="{Binding RealisateurSelecter, Mode=TwoWay}" Grid.Column="3" >
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nr°" Width="30" DisplayMemberBinding="{Binding Id_Personne_real }" ></GridViewColumn>
                                <GridViewColumn Header="Nom" Width="100" DisplayMemberBinding="{Binding Nom_real }"></GridViewColumn>
                                <GridViewColumn Header="Prenom" Width="100" DisplayMemberBinding="{Binding Prenom_real }"></GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <StackPanel Margin="863,244,97,369" Grid.Column="3">
                        <WrapPanel />
                        <Image  Margin="10"  />
                    </StackPanel>
                    <Button  Command="{Binding AjouterImage}" Content="Charger une image" Margin="306,140,639,468" Grid.Column="3" />
                    <Label Grid.Column="3" Content="{Binding ConfirmationFilmAjouter}"  HorizontalAlignment="Left" Margin="120,85,0,0" VerticalAlignment="Top" Width="131" Height="36" />
                    <Label Grid.Column="3" Content="{Binding Id}" HorizontalAlignment="Left" Margin="649,49,0,0" VerticalAlignment="Top" Width="66"/>
                </Grid>
            </TabItem>


            <!-- ////////////////////////////////////////////////////////////Tabitem1 ///////////////////////////////////////// -->
            <!-- ////////////////////////////////////////////////////////////Tabitem1 ///////////////////////////////////////// -->

            <TabItem Header="Ajouter une Peronne dans la liste" HorizontalAlignment="Left" Width="206" Margin="-2,-2,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="24*"/>
                        <ColumnDefinition Width="11*"/>
                        <ColumnDefinition Width="52*"/>
                        <ColumnDefinition Width="1107*"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Margin="5,25,827,556" Text="{Binding NomCreaPersonne}" Grid.Column="3"/>
                    <TextBox Margin="5,155,810,438" Text="{Binding PreNomCreaPersonne}" Grid.Column="3"/>
                    <DatePicker Margin="5,249,810,305" Text="{Binding DateNiassPersonne}" Grid.Column="3" />

                    <Button Command="{Binding AjouterPersonne}" Margin="5,409,914,189" Grid.Column="3">Ajouter</Button>
                    <Label Grid.Column="3"  Content="{Binding ErrorMessageOk}" HorizontalAlignment="Left" Margin="386,25,0,0" VerticalAlignment="Top"  Height="30"/>

                </Grid>
            </TabItem>
            <!-- ////////////////////////////////////////////////// Tabitem 3 ///////////////////////////////////////////////////////////////////////////////-->
            <!-- ////////////////////////////////////////////////// Tabitem 3 ///////////////////////////////////////////////////////////////////////////////-->
            <TabItem  Header="Creer une liste de film">
                <Grid>
                    <TextBox Text="Test" Margin="91,80,998,553"/>
                    <Label Content="Nom de la liste :" HorizontalAlignment="Left" Margin="84,43,0,0" VerticalAlignment="Top" Height="26" Width="98"/>

                    <Label  Content="Sélectionnez un film dans la liste" HorizontalAlignment="Left" Margin="99,152,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.289,0.502"/>

                    <ListView SelectionMode="Single"  Margin="91,204,807,223" ItemsSource="{Binding TousRealisateur}" SelectedItem="{Binding RealisateurSelecter, Mode=TwoWay}" >
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nr°" Width="30" DisplayMemberBinding="{Binding Id_Personne_real }" ></GridViewColumn>
                                <GridViewColumn Header="Nom" Width="100" DisplayMemberBinding="{Binding Nom_real }"></GridViewColumn>
                                <GridViewColumn Header="Prenom" Width="100" DisplayMemberBinding="{Binding Prenom_real }"></GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
